server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests if needed
    # location /api/ {
    #     proxy_pass http://catalyst-core-service:8080/;
    # }
}
