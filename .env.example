# Deleggit Platform Configuration
# Copy this file to .env and adjust values for your local environment.

# --- Platform Identifiers ---
DELEGGIT_ENV=development
# Host OS forces specific sandbox strategies (e.g. windows -> nested-virt checks)
# Options: windows, linux, macos
DELEGGIT_HOST_OS=windows

# --- Kubernetes Configuration ---
# The context name for the local swarm cluster
K8s_CONTEXT=kind-deleggit
# Namespace for agent execution
K8S_NAMESPACE=deleggit-agents
# Runtime Class for untrusted code.
# On Windows + Kind, this might fallback to 'runc' if nested virt is unavailable.
# Recommended: kata-qemu, runsc (gVisor), or runc (INSECURE - Dev only)
K8S_SANDBOX_RUNTIME=kata-qemu

# --- Infrastructure Services ---
# Database for Agent Memory / Core State
DB_URL=postgresql://deleggit:securepassword@localhost:5432/deleggit_core
# Redis for Job Queue
REDIS_URL=redis://localhost:6379/0

# --- Agent Capabilities ---
# LLM Provider Configuration
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Search / Research Tools
# Serper or Tavily for internet access
TAVILY_API_KEY=tvly-...

# --- GitHub Integration ---
# Token for cloning/pushing changes
GITHUB_TOKEN=ghp_...
# The bot user identity for commits
GIT_USER_NAME=Deleggit Bot
GIT_USER_EMAIL=bot@deleggit.local
